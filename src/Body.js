import Hero from './Hero'

function Body() {
    return (
        <body style={{minHeight: '100vh', height:'max-content', marginLeft:'5vw', marginRight:'5vw'}}>
            <Hero />
        </body>
    )
}

export default Body